# .env.example — Required secrets template
# Copy to: src/<your_package>/.env  (NEVER commit .env — add it to .gitignore)
# For local dev: fill real values here.
# For production: leave empty — secrets are resolved via bw-env login shell.

SECRET_ONE=
SECRET_TWO=
