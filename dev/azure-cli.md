# Azure CLI (`az`) — deactivated on KπX workstation

> **Status:** **OFF** — no Azure subscription; package removed. Do not reinstall without explicit KπX decision and an active subscription.

**Microsoft operations:** use **`m365`** only — see `$HOME/KpihX-Labs/Explore/techskills/apps/m365-cli.md` and skill **`k-microsoft`**.

**If Azure is needed later:** reinstall `azure-cli`, restore runbook content from git history of this file, update `k-microsoft` deactivation section.
